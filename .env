# Use your computer's IP instead of localhost when running on Android
# Physical device: use your machine's LAN IP (e.g. 192.168.1.5)
# Android emulator: use 10.0.2.2
EXPO_PUBLIC_API_URL=http://192.168.1.12:8000
